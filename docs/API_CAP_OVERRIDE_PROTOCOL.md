# API Cap Override Protocol (Temporary)

Active window: now through 2026-04-16 reset.

## Why this exists

The API bucket is exhausted for this cycle. This protocol keeps project quality stable while work continues on available model capacity.

## Temporary model routing

1. Default route: Auto + Composer
2. Gemini Flash: codebase lookup and quick discovery only
3. Premium API models: hold until reset unless a critical blocker requires explicit overage approval

## Active scope lock (must follow)

Only work this sequence from `docs/NEXT_TASK_TEST_GATE.md`:
1. Tutorial step-2 visibility stability
2. Color/cartoon pass
3. Star emphasis polish
4. Onboarding wording simplification
5. Fire HD readability sanity pass

No unrelated feature expansion during this temporary window.

## Runtime-evidence debugging rule

For UI bugs and blockers:
- Reproduce first
- Capture runtime evidence (device behavior + logs)
- Ship smallest verified change
- Re-test immediately

If two focused attempts fail without new evidence, stop and carry forward.

## Session usage guardrails

- One technical objective per session
- One verified increment before moving to another objective
- Keep prompts short and scoped
- Avoid broad redesign requests during cap window

## Weekly decision gate

Run every Monday with `docs/MONDAY_API_CAP_CHECKIN_SCRIPT.md`.

Decision outcomes:
- Pace More
- Upgrade Plan
- Keep Moving

Current default mode: **Upgrade Plan** (tighten process, do not reduce quality).

## Roadmap sync during cap window

- Sticker Quest remains active and primary.
- Inkborne governance work is allowed only in short planning bursts.
- The Anchor's Desk stays maintenance-only planning until reset.

## Reset transition

On reset week, execute `docs/API_RESET_TRANSITION_CHECKLIST.md`.
