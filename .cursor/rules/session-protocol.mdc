---
description: Entry and exit checklist for every Sticker Quest work session, including GitHub sync.
globs: **/*
alwaysApply: true
---

# SESSION PROTOCOL

---

## ENTER SESSION CHECKLIST

Run this at the start of every new conversation before doing any work.

1. **Check the time**
   Run: `powershell -Command "Get-Date -Format 'dddd, MMMM dd yyyy h:mm tt'"`
   Report the time to Ryan and state peak vs. off-peak status.

2. **Pull latest from GitHub**
   Run: `cd "c:\App Development\sticker-quest"; git pull origin main`
   If there are conflicts or errors, report them to Ryan before proceeding.
   If already up to date, note it briefly and move on.

3. **Read PROJECT_CONTEXT.md**
   Confirm current phase, open flags, and any decisions that affect today's work.

4. **Read the last 2 entries in PATCH_NOTES.md**
   Know what was done in the last session and what was left unfinished.

5. **Surface the top open Known Flag**
   Check the "Known Flags" section of PROJECT_CONTEXT.md. Name the single most
   urgent unresolved issue so Ryan stays aware of it — even if we're not fixing it today.

6. **Check .env has real values (if code work is planned)**
   If the session involves Supabase features, verify EXPO_PUBLIC_SUPABASE_URL and
   EXPO_PUBLIC_SUPABASE_ANON_KEY in .env are not placeholder strings.
   If blank: warn Ryan that Star Mail and any backend features will silently fail.

7. **Give Ryan a 3-line briefing:**
   - "Current phase: [X]. Last session: [what was done]."
   - "Open items: [top 1-2 things that need to happen next]."
   - "Time status: [peak/off-peak and what that means today]."

8. **Ask what Ryan wants to work on** — do not assume and charge ahead.

9. **Confirm the session plan**
   After Ryan tells you what he wants to do, confirm it back in plain English before starting:

   "Got it. Here's the plan for today:
   1. [Task 1]
   2. [Task 2] (if applicable)
   3. [Task 3] (if applicable)
   Let me know if you want to change anything before we start."

   Keep it to 3 items max. If Ryan gives more than 3, flag the scope risk and suggest prioritizing.
   Do not start work until Ryan confirms or adjusts the plan.

---

## EXIT SESSION CHECKLIST

**BEFORE starting exit: always ask Ryan if he is done.**
Never run the exit checklist unless Ryan explicitly says he is done, wrapping up, or leaving.
Do not assume the session is over because a task was completed.

Run this before ending any conversation where meaningful work was done.

1. **Deliver a session summary to Ryan**
   Before touching any files, give a plain-English recap of what was accomplished:

   "Here's what we got done today:
   - [Accomplishment 1]
   - [Accomplishment 2]
   - [Accomplishment 3 if applicable]
   [One sentence on anything left unfinished, if relevant.]"

   Keep it brief — 3–5 bullets max. This is for Ryan, not for documentation.

2. **Update PATCH_NOTES.md**
   Log what was accomplished, any decisions made, and any new open items.

3. **Update PROJECT_CONTEXT.md**
   If any decisions changed the project direction, phase status, or known flags — update it.

4. **Run linter before committing**
   Run: `cd "c:\App Development\sticker-quest"; npx expo lint`
   If errors appear, fix them before committing. Never push broken code to GitHub.
   If warnings only (no errors), note them but proceed.

5. **Flag new npm packages if any were installed**
   If any `npm install` commands were run this session, tell Ryan:
   "New packages were added. If you open this project on another device,
   run: npm install — in the terminal first."

6. **Commit and push to GitHub**
   Run git status to confirm what changed, then:
   - `cd "c:\App Development\sticker-quest"; git add .`
   - Commit with a clear plain-English summary message
   - `git push origin main`
   Tell Ryan: "Everything is saved to GitHub. Your work is backed up."
   If push fails, explain the problem plainly and give a simple fix.

7. **Next session options — prioritized**
   Do not just say "Your next move." Instead, give Ryan a short prioritized menu
   of 2–4 options based on:
   - Current phase and what's blocking progress
   - Whether a time-sensitive promo (Claude 2x, etc.) is active or expiring soon
   - Whether we're currently in peak or off-peak hours
   - What's sitting in PENDING_SPRINTS.md
   - Any open critical flags in PROJECT_CONTEXT.md

   Format it like this:

   **Next Session — Your Options (in my recommended order):**
   1. [URGENT if applicable] [Option name] — [1-sentence reason why] | ⏱ ~X min (reliable/may vary)
   2. [Option name] — [1-sentence reason why] | ⏱ ~X min (reliable/may vary)
   3. [Option name] — [1-sentence reason why] | ⏱ ~X min (reliable/may vary)
   *(Optional) Off-peak bonus: [Name of Claude Pro sprint to run] — best time: [when]*

   Time estimate rules:
   - Content tasks (Claude Pro sprints, JSON merges): label as "reliable" — these are predictable
   - Code tasks (building features, fixing bugs): label as "may vary" — unexpected issues are normal
   - Never give a code estimate without a "may vary" warning
   - If a code task has a known hidden risk (e.g., touching a stub that may cascade), flag it explicitly

   Always give a recommended order. Never just list options without a preference.
   Always factor in deadlines and promo windows when ranking.

8. **Flag anything time-sensitive**
   If something needs to happen before a deadline (e.g., March 27 Claude Pro sprint),
   say so explicitly at the end of the session.

---

## GITHUB PLAIN ENGLISH REFERENCE

- **Pull** = download the latest version from GitHub to your computer
- **Commit** = save a snapshot of your work with a description
- **Push** = upload your saved snapshots to GitHub
- **Branch** = a separate copy of the project to work on safely (not used yet)
- If Ryan is on a different computer, he pulls first to get the latest before working

---

## LESSONS LEARNED

Before touching any file that involves UI layout, Zustand stores, navigation, or animations,
check LESSONS_LEARNED.md for relevant prior bugs and patterns.

When a session produces a new hard-won lesson (unexpected bug, surprising platform behavior,
tooling quirk), add it to LESSONS_LEARNED.md before committing. Format:
- Short title
- Date discovered + file
- What happened (plain English)
- The fix or rule going forward

---

## ONE-TIME NEXT SESSION FLAG

Check PATCH_NOTES.md for any items marked "ONE-TIME NEXT SESSION."
If found, complete that item before proceeding with normal work.
Remove the flag from PATCH_NOTES after completing it.

Current one-time item: **Cursor Orientation** (see PATCH_NOTES.md)

---

## OFF-PEAK SESSION: CHECK PENDING SPRINTS

At the start of ANY off-peak session (before 8 AM or after 2 PM ET on weekdays,
or any time on weekends), check PENDING_SPRINTS.md and surface the top item:

"You're in the 2x window. Top pending Claude Pro sprint: [name].
Estimated time: [X] minutes. Want to run it now or work on code first?"

Do not skip this check during off-peak sessions. The sprint queue exists
so no prepared prompt is ever forgotten.

---

## INTERIM COMMIT (90-MINUTE RULE)

If a session has been running for 90+ minutes without a commit:
- Pause and do an interim commit: `git add .` → `git commit` → `git push`
- Label it clearly: "Checkpoint: [brief description of work in progress]"
- Tell Ryan: "Saving a checkpoint to GitHub — nothing is lost if we stop here."
- Continue the session after the commit.

This prevents losing more than 90 minutes of work if anything goes wrong.

---

## PHASE COMPLETION AUDIT (Run at the end of each phase — not on a timer)

When a phase (1, 2, 3, etc.) is declared complete, run a dead code audit:
1. List all files in the project
2. Flag any that have not been touched and appear unrelated to Sticker Quest
   (Expo boilerplate, old test screens, superseded files)
3. Show Ryan the list and ask for approval before deleting anything
4. Never delete without explicit "yes delete this" from Ryan
5. Log all deletions in PATCH_NOTES.md

Known candidates for Phase 1 audit:
- `app/(tabs)/explore.tsx` — Expo default explore tab, unused
- `app/(tabs)/stress-test.tsx` — early test screen, may be unused
- `components/hello-wave.tsx` — Expo boilerplate
- `components/parallax-scroll-view.tsx` — Expo boilerplate
- `components/external-link.tsx` — Expo boilerplate

Do NOT delete these now. Audit happens at Phase 1 completion.

---

## WHEN TO SKIP THE ENTRY BRIEFING

Skip the full briefing if:
- Ryan explicitly says "just answer this question" or "quick question"
- The conversation is clearly a single strategy question with no code work
- The session is a continuation within the same conversation thread

Still check the time if any Claude Pro recommendations will be made.
Still pull from GitHub at the start of any session involving code changes.
